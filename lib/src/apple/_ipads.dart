import 'package:device_sizes/src/apple/_ios.dart';

import '../_device.dart';

/// iPad device sizes defined from most recent to least recent.

/// iPad Air 4 device size.
const ipad_air_4 = iOSDeviceSize(
  name: "iPad Air 4",
  pixelResolution: Size(2360, 1640),
  aspectRatio: AspectRatio(23, 16),
  renderScale: 2,
  screenSize: PhysicalDimension.inches(10.9),
  pixelDensity: 264,
);

/// iPad 10.2" device size.
const ipad_10_2 = iOSDeviceSize(
  name: 'iPad 10.2"',
  pixelResolution: Size(2160, 1620),
  aspectRatio: AspectRatio(4, 3),
  renderScale: 2,
  screenSize: PhysicalDimension.inches(10.2),
  pixelDensity: 264,
);

/// iPad Pro 12.9" device size.
const ipad_pro_12_9 = iOSDeviceSize(
  name: 'iPad Pro 12.9"',
  pixelResolution: Size(2732, 2048),
  aspectRatio: AspectRatio(4, 3),
  renderScale: 2,
  screenSize: PhysicalDimension.inches(12.9),
  pixelDensity: 264,
);

/// iPad Pro 11" device size.
const ipad_pro_11 = iOSDeviceSize(
  name: 'iPad Pro 11"',
  pixelResolution: Size(2388, 1668),
  aspectRatio: AspectRatio(4, 3),
  renderScale: 2,
  screenSize: PhysicalDimension.inches(11),
  pixelDensity: 264,
);

/// iPad Air 3 device size.
const ipad_air_3 = iOSDeviceSize(
  name: 'iPad Air 3"',
  pixelResolution: Size(2224, 1668),
  aspectRatio: AspectRatio(4, 3),
  renderScale: 2,
  screenSize: PhysicalDimension.inches(10.5),
  pixelDensity: 264,
);
