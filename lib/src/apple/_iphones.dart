import '../_device.dart';
import '_ios.dart';

/// iPhone device sizes defined from most recent to least recent.

/// iPhone 12 Pro Max device size.
const iphone_12_pro_max = iOSDeviceSize(
  name: "iPhone 12 Pro Max",
  pixelResolution: Size(2778, 1284),
  aspectRatio: AspectRatio(19.5, 9),
  renderScale: 3,
  screenSize: PhysicalDimension.inches(6.7),
  pixelDensity: 458,
);

/// iPhone 12 Pro device size.
const iphone_12_pro = iOSDeviceSize(
  name: "iPhone 12 Pro",
  pixelResolution: Size(2532, 1170),
  aspectRatio: AspectRatio(19.5, 9),
  renderScale: 3,
  screenSize: PhysicalDimension.inches(6.1),
  pixelDensity: 466,
);

/// iPhone 12 device size.
const iphone_12 = iOSDeviceSize(
  name: "iPhone 12",
  pixelResolution: Size(2532, 1170),
  aspectRatio: AspectRatio(19.5, 9),
  renderScale: 3,
  screenSize: PhysicalDimension.inches(6.1),
  pixelDensity: 460,
);

/// iPhone 12 Mini device size.
const iphone_12_mini = iOSDeviceSize(
  name: "iPhone 12 Mini",
  pixelResolution: Size(2340, 1080),
  aspectRatio: AspectRatio(19.5, 9),
  renderScale: 3,
  screenSize: PhysicalDimension.inches(5.4),
  pixelDensity: 476,
);

/// iPhone 11 Pro Max device size.
const iphone_11_pro_max = iOSDeviceSize(
  name: "iPhone 11 Pro Max",
  pixelResolution: Size(2688, 1242),
  aspectRatio: AspectRatio(19.5, 9),
  renderScale: 3,
  screenSize: PhysicalDimension.inches(6.5),
  pixelDensity: 458,
);

/// iPhone 11 Pro device size.
const iphone_11_pro = iOSDeviceSize(
  name: "iPhone 11 Pro",
  pixelResolution: Size(2436, 1125),
  aspectRatio: AspectRatio(19.5, 9),
  renderScale: 3,
  screenSize: PhysicalDimension.inches(5.8),
  pixelDensity: 458,
);

/// iPhone 11 device size.
const iphone_11 = iOSDeviceSize(
  name: "iPhone 11",
  pixelResolution: Size(1792, 828),
  aspectRatio: AspectRatio(19.5, 9),
  renderScale: 3,
  screenSize: PhysicalDimension.inches(6.1),
  pixelDensity: 458,
);
