import '../_device.dart';

/// Samsung phone device sizes defined from most recent to least recent.

/// Google Pixel 6 Pro device size.
const galaxy_s20_fe = DeviceSize(
  name: "Samsung Galaxy S20 FE",
  pixelResolution: Size(2400, 1080),
  aspectRatio: AspectRatio(20, 9),
  screenSize: PhysicalDimension.inches(6.5),
  pixelDensity: 405,
);
