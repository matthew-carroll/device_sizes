import '../_device.dart';

/// Google phone device sizes defined from most recent to least recent.

/// Google Pixel 6 Pro device size.
const pixel_6_pro = DeviceSize(
  name: "Google Pixel 6 Pro",
  pixelResolution: Size(3120, 1440),
  aspectRatio: AspectRatio(19.5, 9),
  screenSize: PhysicalDimension.inches(6.7),
  pixelDensity: 512,
);

/// Google Pixel 6 device size.
const pixel_6 = DeviceSize(
  name: "Google Pixel 6",
  pixelResolution: Size(2400, 1080),
  aspectRatio: AspectRatio(20, 9),
  screenSize: PhysicalDimension.inches(6.4),
  pixelDensity: 411,
);

/// Google Pixel 5a device size.
const pixel_5a = DeviceSize(
  name: "Google Pixel 5a",
  pixelResolution: Size(2400, 1080),
  aspectRatio: AspectRatio(20, 9),
  screenSize: PhysicalDimension.inches(6.34),
  pixelDensity: 413,
);

/// Google Pixel 4a device size.
const pixel_4a = DeviceSize(
  name: "Google Pixel 4a",
  pixelResolution: Size(2340, 1080),
  aspectRatio: AspectRatio(19.5, 9),
  screenSize: PhysicalDimension.inches(5.8),
  pixelDensity: 443,
);
